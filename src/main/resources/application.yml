server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rseroter/pluralsight-spring-cloudconfig-wa-tolls-perf.git
          #username: uname
          #password: pass
          search-paths:
            - 'station*'
